class Solution(object):
    def twoSum(self, nums, target):
        nums2 = nums
        for i in range(0, len(nums)):
            for j in range(0, len(nums)):
                sums = nums[i] + nums[j]
                if sums == target and i != j:
                    return i,j
