class Solution(object):
    def arraySign(self, nums):
        if 0 in nums:
            return 0
        elif len([i for i in nums if i < 0]) % 2 ==0:
            return 1
        else:
            return -1
